{% extends 'base.html.twig' %}

{% block title %}TypeOfEventProject{% endblock %}

{% block body %}
    <h1>TypeOfEventProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_event_project.id }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ type_of_event_project.action }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_of_event_project_index') }}">back to list</a>

    <a href="{{ path('type_of_event_project_edit', {'id': type_of_event_project.id}) }}">edit</a>

    {{ include('type_of_event_project/_delete_form.html.twig') }}
{% endblock %}
