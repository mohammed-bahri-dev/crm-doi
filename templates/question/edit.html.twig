{% extends 'base.html.twig' %}

{% block title %}Edit Question{% endblock %}

{% block body %}
    <h1 style="background-color:#138496;color:white; text-shadow: 0px 0px 2px black;">&nbsp;Questions</h1>

    <p>→ Modifier la question n°{{ question.id }}</p>

    {{ include('question/_form.html.twig', {'button_label': 'Valider'}) }}
    <br>
    <a href="{{ path('question_index') }}">&#8592; Retour à la liste</a>
    <br><br>
    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
