{% extends 'base.html.twig' %}

{% block title %}TypeOfCaEntity{% endblock %}

{% block body %}
    <h1>TypeOfCaEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_ca_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_of_ca_entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_of_ca_entity_index') }}">back to list</a>

    <a href="{{ path('type_of_ca_entity_edit', {'id': type_of_ca_entity.id}) }}">edit</a>

    {{ include('type_of_ca_entity/_delete_form.html.twig') }}
{% endblock %}
