{% extends 'base.html.twig' %}

{% block title %}Edit PartnerStatus{% endblock %}

{% block body %}
    <h1 style="background-color:#138496;color:white; text-shadow: 0px 0px 2px black;">&nbsp;Statuts de partenaires</h1>

    <p>→ Modifier le statut n°{{ partner_status.id }}</p>

    {{ include('partner_status/_form.html.twig', {'button_label': 'Valider'}) }}
    <br>
    <a href="{{ path('partner_status_index') }}">&#8592; Retour à la liste</a>
    <br><br>
    {{ include('partner_status/_delete_form.html.twig') }}
{% endblock %}
