{% extends 'base.html.twig' %}

{% block title %}ParticipationEvent{% endblock %}

{% block body %}
    <h1>ParticipationEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participation_event.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participation_event_index') }}">back to list</a>

    <a href="{{ path('participation_event_edit', {'id': participation_event.id}) }}">edit</a>

    {{ include('participation_event/_delete_form.html.twig') }}
{% endblock %}
