<form method="post" action="{{ path('project_status_delete', {'id': project_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_status.id) }}">
    <button class="btn">Supprimer</button>
</form>
