{% extends 'base.html.twig' %}

{% block title %}TypeOfEvent{% endblock %}

{% block body %}
    <h1>TypeOfEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_event.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_of_event.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_of_event_index') }}">back to list</a>

    <a href="{{ path('type_of_event_edit', {'id': type_of_event.id}) }}">edit</a>

    {{ include('type_of_event/_delete_form.html.twig') }}
{% endblock %}
