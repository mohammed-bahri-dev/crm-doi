{% extends 'base.html.twig' %}

{% block title %}TypeOfProject{% endblock %}

{% block body %}
    <h1>TypeOfProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_project.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_of_project.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_of_project_index') }}">back to list</a>

    <a href="{{ path('type_of_project_edit', {'id': type_of_project.id}) }}">edit</a>

    {{ include('type_of_project/_delete_form.html.twig') }}
{% endblock %}
