{% extends 'base.html.twig' %}

{% block title %}CaEntity{% endblock %}

{% block body %}
    <h1>CaEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ca_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ca_entity.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ ca_entity.Address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ca_entity_index') }}">back to list</a>

    <a href="{{ path('ca_entity_edit', {'id': ca_entity.id}) }}">edit</a>

    {{ include('ca_entity/_delete_form.html.twig') }}
{% endblock %}
