{% extends 'base.html.twig' %}

{% block title %}PreferredContact{% endblock %}

{% block body %}
    <h1>PreferredContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preferred_contact.id }}</td>
            </tr>
            <tr>
                <th>Mode</th>
                <td>{{ preferred_contact.mode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('preferred_contact_index') }}">back to list</a>

    <a href="{{ path('preferred_contact_edit', {'id': preferred_contact.id}) }}">edit</a>

    {{ include('preferred_contact/_delete_form.html.twig') }}
{% endblock %}
