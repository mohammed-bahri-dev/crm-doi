{% extends 'base.html.twig' %}

{% block title %}ProjectStatus{% endblock %}

{% block body %}
    <h1>ProjectStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_status.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ project_status.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_status_index') }}">back to list</a>

    <a href="{{ path('project_status_edit', {'id': project_status.id}) }}">edit</a>

    {{ include('project_status/_delete_form.html.twig') }}
{% endblock %}
