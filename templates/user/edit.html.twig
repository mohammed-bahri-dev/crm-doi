{% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
    <h1 style="background-color:#138496;color:white; text-shadow: 0px 0px 2px black;">&nbsp;Utilisateurs</h1>

    <p>→ Mise à jour des données de l'utilisateur n°{{ user.id }}</p>

    {{ include('user/_form.html.twig', {'button_label': 'Valider'}) }}
    <br>
    <a href="{{ path('user_index') }}">&#8592; Retour à la liste</a>
    <br><br>
    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
