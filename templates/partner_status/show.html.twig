{% extends 'base.html.twig' %}

{% block title %}PartnerStatus{% endblock %}

{% block body %}
    <h1>PartnerStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partner_status_index') }}">back to list</a>

    <a href="{{ path('partner_status_edit', {'id': partner_status.id}) }}">edit</a>

    {{ include('partner_status/_delete_form.html.twig') }}
{% endblock %}
